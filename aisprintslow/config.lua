Config = {}

-- 1.0 = normal. 0.90 = 10% slower sprint.
Config.SprintMultiplier = 0.90

-- How often to rescan peds (ms). Lower = more responsive, higher = less CPU.
Config.ScanIntervalMs = 750

-- Only consider peds within this radius of the player (meters-ish).
Config.Radius = 80.0

-- If true, will also affect mission peds (often you do NOT want this).
Config.IncludeMissionPeds = false
